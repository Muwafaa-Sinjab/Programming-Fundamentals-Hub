#include <iostream>

using namespace std;

short ReadYear(); 
short ReadMonth();
short ReadDay();
bool IsleapYear(short year);

short NumberOfDaysInMonth(short year, short month);
short DayOfYear(short year, short month, short day);

int main()
{
    short day = ReadDay();
    short month = ReadMonth();
    short year = ReadYear();
    short numberOfDays = DayOfYear(year, month, day);

    cout << "\nNumber of Days From Begining of the year is: " << numberOfDays << endl;
    return 0;
}

short ReadYear()
{
    short year = 0;
    cout << "Please Enter to check year: ";
    cin >> year;
    return year;
}

short ReadMonth()
{
    short month = 0;
    cout << "Please Enter to check month: ";
    cin >> month;
    return month;
}

short ReadDay()
{
    short day = 0;
    cout << "Please Enter to check Day: ";
    cin >> day;
    return day;
}


bool IsleapYear(short year)
{
    return (year % 4 == 0 && year % 100 != 0 ) || (year % 400 == 0);
}

short NumberOfDaysInMonth(short year, short month)
{
    if (month < 1 || month > 12) return 0;

    short arrNumberOfDays[12] = {31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31};
    return (month == 2) ? (IsleapYear(year) ? 29 : 28) : arrNumberOfDays[month - 1];
}

short DayOfYear(short year, short month, short day)
{
    short totalDays = 0;

    for(short i = 1; i < month ; i++)
    {
        totalDays += NumberOfDaysInMonth(year, i);
    }
    
    return (totalDays + day);
}