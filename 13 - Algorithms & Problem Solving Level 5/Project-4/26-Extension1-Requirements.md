# 26 - (Extension 1) - `Resize` Function 

## Overview

This extension adds a `Resize(int NewSize)` function to the `clsMyDynamicArray` class.

## Main Topics

### `Resize`

The function changes the array size to the new size `NewSize`.

## Key Takeaways

* Add a `Resize(int NewSize)` function.
* The function changes the array size.

[Muwafaa Sinjab] @[**Muwafaa-Sinjab**](https://github.com/Muwafaa-Sinjab)