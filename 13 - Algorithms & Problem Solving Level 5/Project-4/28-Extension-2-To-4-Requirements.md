# 28 - (Extension 2 - 4) - Requirements

## Overview

This extension adds new functions to the `clsMyDynamicArray` class.

## Main Topics

### `GetItem(Index)`

Add a `GetItem(Index)` function.

### `Reverse()`

Add a `Reverse()` function.

### `Clear()`

Add a `Clear()` function.

## Key Takeaways

* Add `GetItem(Index)`.
* Add `Reverse()`.
* Add `Clear()`.

[Muwafaa Sinjab] @[**Muwafaa-Sinjab**](https://github.com/Muwafaa-Sinjab)

```
```
