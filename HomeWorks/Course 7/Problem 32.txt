#include <iostream>
#include <cctype>

using namespace std;
enum enVowelsCharactar {a = 'a', e = 'e', i = 'i', o = 'o', u = 'u'};


char readChar(string message);
bool IsVowelCharacter(enVowelsCharactar enVowel);
bool IsVowelCharacter(char ch);


int main()
{
    char ch = readChar("Please Enter a character: ");

    if(IsVowelCharacter(ch))
        cout << "\nYES, Letter '" << ch << "' is vowel." << endl;
    else
        cout << "\nNO, Letter '" << ch << "' is  NOT vowel." << endl;
    return 0;
}

char readChar(string message)
{
    char input;
    cout << message;
    cin >> input;
    return input;
}

bool IsVowelCharacter(char ch)
{
    ch = tolower(ch);

    return(ch == enVowelsCharactar::a ||  
       ch == enVowelsCharactar::e ||
       ch == enVowelsCharactar::i ||  
       ch == enVowelsCharactar::o ||
       ch == enVowelsCharactar::u);
}
