#include <iostream>
#include <string>

using namespace std;

int ReadNumber();
string NumberToText(int number);

int main()
{
    int number = ReadNumber();
    cout << "[" << NumberToText(number) << "]";
    cout <<"\n";
    return 0;
}

string NumberToText(int number)
{   
    if(number <= 9)
    {
        string onesPlace[] = {" ", "one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};
        return onesPlace[number];

    }
    else if(number <= 19)
    {
        string teens[] ={"ten", "eleven", "twelve", "thirteen", "fourteen", "fifteen", "sixteen", "seventeen", "eighteen", "nineteen"};
        return teens[number - 10];
    }
    else if(number <= 99)
    {
        string tensPlace[] ={ "", "", "twenty", "thirty", "forty", "fifty", "sixty", "seventy", "eighty", "ninety"};

        if(number % 10 == 0)
        {
            return tensPlace[number / 10];
        }
        return tensPlace[number / 10] + " " + NumberToText(number % 10);
    }
    else if(number <= 999)
    {
        string hundredsPlace[] ={"", "one hundred", "two hundred", "three hundred", "four hundred",
        "five hundred", "six hundred", "seven hundred", "eight hundred", "nine hundred"};
        if(number % 100 == 0)
        {
            return hundredsPlace[number / 100];
        }
        return hundredsPlace[number / 100] + " " + NumberToText(number % 100);
    }
    else if(number <= 9999)
    {
        if(number % 1000 == 0)
        {
            return NumberToText(number / 1000) + " " + "thousand";
        }
        return NumberToText(number / 1000) + " " + "thousand" + " " + NumberToText(number % 1000);
    }
    else if(number <= 99999)
    {
       if(number % 1000 == 0)
       {
        return NumberToText(number / 1000) + " " + "thousand";
       }
       return NumberToText(number / 1000) + " " + "thousand" +  " " + NumberToText(number % 1000);
    }
    

   return "None";
}

int ReadNumber()
{
    int num = 0;
    cout << "Enter a number: ";
    cin >> num;
    return num;
}